# Finance Analytics Platform

A simple, deployable finance analytics portfolio application built around the supplied Excel datasets.

## Stack

- Backend: Python, FastAPI, Pandas, SQLAlchemy, Pydantic, psycopg
- Database: PostgreSQL
- Frontend: HTML, CSS, Vanilla JavaScript, Plotly.js
- ETL: Pandas + openpyxl streaming batches
- AI: backend-only LLM HTTP integration; no API key reaches the browser

## Dataset

The supplied files are stored in `data_pipeline/data/raw/`:

- `customers.xlsx` — 60,000 rows, 12 columns
- `accounts(1).xlsx` — 95,000 rows, 7 columns
- `transactions.xlsx` — 1,048,575 rows, 7 columns

The production API never reads Excel files. Excel is an ETL input only.

## Architecture

```text
Excel files
   |
   v
Extract -> Validate -> Transform -> Load
                                   |
                                   v
                              PostgreSQL
                                   |
                         +---------+---------+
                         |                   |
                    FastAPI APIs         SQL analytics
                         |
                  JSON responses
                         |
                         v
              Static HTML/CSS/JS + Plotly
                         |
                         v
                    FinAI endpoint
                         |
                         v
                  LLM provider API
```

## Data model

```text
customers (1) ----< accounts (1) ----< transactions
```

`customers.customer_id` is the customer primary key. `accounts.customer_id` references it. `transactions.account_id` references accounts.

Phone numbers are stored as text because they are identifiers, not quantities. Transaction amounts are stored as `NUMERIC(18,2)`; their sign is preserved because transaction direction is represented by the dataset's transaction type.

## Quick start

### 1. PostgreSQL

Create a database and user, then copy `.env.example` to `.env` and update `DATABASE_URL`.

Optional local PostgreSQL with Docker Compose is included, but Docker is not required for deployment.

### 2. Install backend dependencies

```bash
cd backend
python -m venv .venv
# Windows: .venv\\Scripts\\activate
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Run ETL

From the project root:

```bash
python -m data_pipeline.run_pipeline
```

The pipeline validates and loads customers first, then accounts, then transactions. It uses streaming workbook reads for the large transaction workbook and PostgreSQL COPY for efficient batch loading.

Useful options:

```bash
python -m data_pipeline.run_pipeline --reset
python -m data_pipeline.run_pipeline --validate-only
```

### 4. Start API

```bash
cd backend
uvicorn app.main:app --reload --port 8000
```

Open `http://localhost:8000/docs` for Swagger UI.

### 5. Start frontend

From the `frontend/` directory:

```bash
python -m http.server 5500
```

Open `http://localhost:5500`.

Set `frontend/js/config.js`'s `API_BASE_URL` for a deployed backend.

## API

- `GET /api/health`
- `GET /api/analytics/python/summary`
- `GET /api/analytics/python/monthly-transactions`
- `GET /api/analytics/python/transaction-types`
- `GET /api/analytics/python/channels`
- `GET /api/analytics/python/merchant-categories`
- `GET /api/analytics/python/income-activity`
- `GET /api/analytics/sql/customer-summary`
- `GET /api/analytics/sql/account-summary`
- `GET /api/analytics/sql/transaction-summary`
- `GET /api/analytics/sql/customer-360`
- `POST /api/ai/chat`

Analytics filters are query parameters and are validated by FastAPI/Pydantic. Customer-facing APIs deliberately return anonymous customer labels rather than names, phones, or emails.

## AI safety model

FinAI v1 sends a controlled analytics context to the model. It does not execute arbitrary SQL generated by the user. Database credentials and the LLM key remain server-side. A future read-only natural-language-to-SQL feature should use a dedicated database role, strict SQL parsing/allow-listing, query timeouts, and row/column restrictions.

## Testing

```bash
pytest -q
```

Tests cover transformation/validation helpers and basic FastAPI validation behavior. Full integration tests require PostgreSQL populated by the ETL pipeline.

## Deployment

Recommended simple production layout:

```text
Static host (frontend) ----HTTPS----> FastAPI host ----TLS----> Managed PostgreSQL
                                      |
                                      +----> LLM API
```

Use a managed PostgreSQL service and a Python/FastAPI host. Set `API_BASE_URL` in the frontend configuration and `DATABASE_URL`, `CORS_ORIGINS`, and AI settings as backend environment variables.

## Privacy

The source data contains customer PII. Do not publish the raw Excel files or expose name, phone, or email in public endpoints. If publishing this portfolio publicly, replace the supplied data with synthetic/anonymized data or keep the data private.
## Final portfolio updates

- Home introduction is concise and positions Geoffrey as a Machine Learning / AI / Automation Engineer and Data Analyst.
- Technical toolkit is intentionally placed before professional experience and highlights Python, Pandas, SQL/PostgreSQL, ETL, AWS ML, n8n, RAG/AI, Grafana and engineering skills.
- Current Equity Bank experience now highlights the published n8n fraud-detection automation.
- Python Insights and SQL Insights include interactive fraud-risk dashboards with threat levels, transaction value/volume, channel exposure and peak analysis.
- Fraud views use an explainable rule-based **risk proxy**, not a confirmed fraud label, because the supplied transactions dataset does not contain ground-truth fraud labels.
- Dashboard filters remain available for date, transaction type, channel, merchant category and account type.
- The visual system uses restrained blue, slate and light-grey gradients rather than a highly saturated palette.



## Latest performance and visual updates
- Unified the portfolio around a restrained dynamic blue-grey/light-grey design system across Home, Professional Snapshot, insights, FinAI, About, resume/contact areas and footers.
- Added GitHub (`https://github.com/Jeffgmw`) and LinkedIn (`http://www.linkedin.com/in/geoffrey-maina-310548127`) profile links.
- Reduced Home section spacing for a tighter portfolio flow.
- Reduced dashboard startup work by loading critical KPI/trend/risk charts first and deferring secondary charts until the browser is idle.
- Consolidated filter-option loading into one API request and added composite PostgreSQL indexes for common dashboard filters/time-series joins.
- Restricted Plotly modebar controls to zoom in, zoom out, download plot and autoscale; pan, selection, lasso and reset controls are removed.
- Fixed the filtered summary channel query so its bind parameters are passed correctly.
